import { Easing } from 'react-native';

// Layout constants
export const center = {
  top: 8,
  left: 30,
};

export const topCenter = {
  top: -70,
  left: -30,
};

export const topCenter2 = {
  top: -70,
  left: 30,
};

export const topLeft = {
  top: -40,
  left: -80,
};

export const topRight = {
  top: -40,
  left: 80,
};


// Style constants
export const bigBubbleSize = 60;
export const smallBubbleSize = 50;
export const bubbleColor = 'goldenrod';

// Animate Constants
export const animateTime = 800;
export const easingType = Easing.out(Easing.exp);
export const delay = 200;
